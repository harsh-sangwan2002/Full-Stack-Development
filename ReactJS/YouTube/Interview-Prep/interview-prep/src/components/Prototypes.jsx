import React from 'react'
import PropTypes from 'prop-types'

function Prototypes({ val }) {
    return (
        <div>
        </div>
    )
}

export default Prototypes

Prototypes.propTypes = {
    val: PropTypes.string
}
